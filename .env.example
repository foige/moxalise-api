# Application settings
PROJECT_NAME="Moxalise API"
PROJECT_DESCRIPTION="API for interacting with Moxalise Google Spreadsheet"
VERSION="0.1.0"
API_PREFIX="/api"

# CORS settings
# Comma-separated list of origins that are allowed to make cross-origin requests
# Example: CORS_ORIGINS=http://localhost:3000,https://example.com
CORS_ORIGINS=http://localhost:3000

# Google Sheets settings
# The ID of the Google Spreadsheet to interact with
# This is the long string in the URL of your spreadsheet:
# https://docs.google.com/spreadsheets/d/{SPREADSHEET_ID}/edit
GOOGLE_SHEETS_SPREADSHEET_ID=your_spreadsheet_id_here

# Security settings
# Salt used for hashing IP addresses before storing them
IP_HASH_SALT=your_random_salt_string_here

# Note: Authentication is handled using Application Default Credentials (ADC)
# For local development, run: gcloud auth application-default login
# In Cloud Run, the service account credentials are automatically available